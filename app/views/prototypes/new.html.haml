
.container.proto-new
  = form_for @prototype do |f|
    .col-md-8.col-md-offset-2
      %header.row.user-nav.row
        .col-md-12
          = f.text_field :title, placeholder: 'Title（必須）', class: 'proto-new-title'
      = f.fields_for :captured_images do |image|
        .row
          .col-md-12
            .cover-image-upload
              = image_tag (), { id: 'main_image_preview', class: 'col-md-12' }
              = image.file_field :main, required: true
          .col-md-12
            %ul.proto-sub-list.list-group
              - 1.upto(3) do |i|
                %li.list-group-item.col-md-4
                  .image-upload
                    = image_tag (), id: "sub_#{i}"
                    = image.file_field :"sub#{i}"
              %li.list-group-item.col-md-4#hide_form
                .image-upload
                  = image_tag (), { id: 'sub_4'}
                  = image.file_field :sub4
              %li.list-group-item.col-md-4#add_new_form
                .image-upload-plus
                  %span +
        .row.proto-description
          .col-md-12
            = f.text_field :catch_copy, placeholder: 'Catch Copy(必須)'
          .col-md-12
            = f.text_area :concept, cols: '30', rows: '4', placeholder: 'Concept（必須）'
        = f.fields_for :tags do |tag|
          .col-md-12
            %h4 Tag List
            .row
              .col-md-3
                = tag.text_field :'0'
              .col-md-3
                = tag.text_field :'1'
              .col-md-3
                = tag.text_field :'2'
        .row.text-center.proto-btn
          = f.submit 'PUBLISH', class: 'btn btn-primary btn-block'

