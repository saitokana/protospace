.container.tag-container
  .row
    - @tags.each do |tag|
      - if tag.taggings_count > 0
        .tag-list
          .col-sm-4
            = link_to tag.name, tag_path(tag, tag_name: tag.name), class: "btn tag-btn"
